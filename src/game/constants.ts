/** Level generator & canvas share: min angular gap between stuck arrows (deg). */
export const MIN_ARROW_GAP_DEG = 14;

/** Collision: minimum circular separation required (deg). Slightly below gap so tight levels still work. */
export const COLLISION_SEPARATION_DEG = 5.5;

/** Turns abstract level rotationSpeed into visible deg/sec on device. */
export const ROTATION_SPEED_SCALE = 46;
